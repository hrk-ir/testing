<!DOCTYPE html>
<html>
    <head>
        <title>projects show {{ $project->title }}</title>
    </head>

    <body>

        <h1>{{ $project->title }}</h1>

        <p>{{ $project->description }}</p>
    </body>
</html>
